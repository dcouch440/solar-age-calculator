import '../css/styles.css';
import {SolarAge} from './SolarAge.js';

// const eventLogger = (event, age) => {

//   const currentEvent = 
//   {
//     Earth: () =>  age.getEarthAge(),
//     Mercury: () => age.getMercuryAge(),
//     Venus: () => age.getVenusAge(),
//     Mars: () => age.getMarsAge(),
//     Jupiter: () => age.getJupiterAge(),
//     Default: () => null
//   };
//   return (currentEvent['cats']() || currentEvent['Default']());
// };

SolarAge;
document.addEventListener("DOMContentLoaded", () => {
  const dropDownValue = document.getElementById('options').value;

});
